---
import BaseLayout from '../layouts/BaseLayout.astro';
import QuoteOfTheDay from '../components/QuoteOfTheDay.astro'
import { getArticles } from '../utils/getArticles';


const STATICPATH = "";
const SITENAME = "На струнах трепетных гуслей";
const SITETAGLINE = "Мир поэзии Анатолия Яни";
const SITEURL = "";
const title = "Анатолий Яни";

let articles = await getArticles();
articles = articles.slice(0, 5);

const AUTOBIOGRAPHY_EXCERPT = `Многие факты моей бьющей петергофскими фонтанами биографии по разным причинам остаются как бы в непроглядном тумане. Но всё, что я ещё в состоянии вспомнить, попробую излить. Лаконично, сжато, конспективно.`;
const CREATIVE_PROCESS_EXCERPT = `Для Анатолия Яни творческий процесс был "звенящей бессонницей" - неугомонным, неустанным стремлением запечатлеть чувства и впечатления в поэтической форме. "Я пишу стихи так, как другие дышат воздухом", - говорил он. Его душа была "роддомом стихометафор", а сам он - "главным акушером" в этом удивительном мире поэзии.`;


---
<BaseLayout>
  <Fragment slot="extra_head">
    <link rel="stylesheet" href={`${STATICPATH}/styles/home.css`} />
  </Fragment>
  <Fragment slot="body">
    <section class="hero with-gradient">
        <div class="hero-text-container">
            <h1>{SITENAME}</h1>
            <h2>{SITETAGLINE}</h2>
            <p>Добро пожаловать в мир, где слова плетут узоры нежнее, чем кружева, где рифмы звенят, как струны гуслей, а метафоры сверкают ярче звёзд.</p>
            <a href="category/poems.html" class="hero-cta btn btn-primary btn-cta">Читать стихи</a>
        </div>
    </section>

    <nav class="home-nav">
        <ul>
            <li><a href={`${SITEURL}/about.html`}>О поэте</a></li>
            <li><a href={`${SITEURL}/category/poems.html`}>Нетронутый Грааль</a></li>
            <li><a href={`${SITEURL}/category/translations.html`}>Вино из грозди сердца</a></li>
            <li><a href={`${SITEURL}/creative-process.html`}>Звенящая бессонница</a></li>
        </ul>
    </nav>

    <main>
      <QuoteOfTheDay />

      <section class="content-section">
          <h3>Последние публикации</h3>
          <ul>
              {articles.map(article => (
                  <li><a href={`${SITEURL}${article.url}`}>{article.title}</a></li>
              ))}
          </ul>
      </section>

      <section class="cta-content-section">
          <div class="content-wrapper">
              <h3>Автобиография</h3>
              <p>{AUTOBIOGRAPHY_EXCERPT}</p>
          </div>
          <a class="btn btn-primary btn-cta" href={`${SITEURL}/without-a-shadow-of-fiction.html`}>Читать полностью</a>
      </section>

      <section class="cta-content-section">
          <div class="content-wrapper">
            <h3>Творческий процесс</h3>
            <p>{CREATIVE_PROCESS_EXCERPT}</p>
          </div>
          <a class="btn btn-primary btn-cta" href={`${SITEURL}/creative-process.html`}>Узнать больше</a>
      </section>

      <section class="content-section">
          <h3>Разделы</h3>
          <ul>
              <li><a href={`${SITEURL}/category/poems.html`}>Стихи</a></li>
              <li><a href={`${SITEURL}/category/translations.html`}>Переводы</a></li>
              <li><a href={`${SITEURL}/palindromes.html`}>Палиндромы</a></li>
          </ul>
      </section>

    </main>
  </Fragment>
</BaseLayout>
